var SsmlUtilities = /** @class */ (function () {
    function SsmlUtilities() {
    }
    SsmlUtilities.isPlainText = function (ssml) {
        return !/(?:(<[^>]*[/]>)|(<[^>]*>.*?<[/][^>]*>))/g.test(ssml);
    };
    SsmlUtilities.buildAudioTag = function (src) {
        return "<audio src=\"" + src + "\"/>";
    };
    SsmlUtilities.isSSML = function (text) {
        return /^<speak>.*<\/speak>$/.test(text);
    };
    SsmlUtilities.toSSML = function (text) {
        text = text.replace(/<[/]?speak>/g, '');
        return "<speak>" + text + "</speak>";
    };
    SsmlUtilities.removeSSMLSpeakTags = function (ssml) {
        return ssml.replace(/<[/]?speak>/g, '');
    };
    SsmlUtilities.removeSSML = function (ssml) {
        return ssml.replace(/<[^>]*>/g, '');
    };
    return SsmlUtilities;
}());
export { SsmlUtilities };
//# sourceMappingURL=SsmlUtilities.js.map