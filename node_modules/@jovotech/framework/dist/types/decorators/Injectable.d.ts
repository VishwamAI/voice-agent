import { AnyObject, Constructor } from '@jovotech/common';
import { InjectableOptions } from '../metadata/InjectableMetadata';
/**
 * Decorator to mark a class as injectable.
 * This allows the class to be automatically constructed by the dependency injection system.
 * @param options
 */
export declare function Injectable<PROVIDER = AnyObject>(options?: InjectableOptions): (target: Constructor<PROVIDER>) => void;
