import { Jovo } from './Jovo';
import { ArrayElement, Constructor } from '@jovotech/common';
import { InjectionToken } from './metadata/InjectableMetadata';
export interface DependencyTree<Node> {
    token: InjectionToken;
    resolvedValue: Node;
    children: DependencyTree<Node extends Constructor<any, infer Args> ? ArrayElement<Args> : unknown>[];
}
export declare class DependencyInjector {
    private static resolveInjectionToken;
    private static instantiateClassWithTracing;
    static instantiateClass<TYPE, ARGS extends unknown[] = []>(jovo: Jovo, clazz: Constructor<TYPE>, ...predefinedArgs: ARGS): Promise<TYPE>;
}
