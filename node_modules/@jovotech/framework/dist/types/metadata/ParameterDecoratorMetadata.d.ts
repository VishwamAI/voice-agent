import { AnyObject, Constructor } from '@jovotech/common';
import { ClassDecoratorMetadata } from './ClassDecoratorMetadata';
export declare abstract class ParameterDecoratorMetadata<TARGET = AnyObject> extends ClassDecoratorMetadata {
    readonly target: Constructor<TARGET> | Function;
    readonly index: number;
    protected constructor(target: Constructor<TARGET> | Function, index: number);
    hasSameTargetAs(otherMetadata: ParameterDecoratorMetadata): boolean;
}
