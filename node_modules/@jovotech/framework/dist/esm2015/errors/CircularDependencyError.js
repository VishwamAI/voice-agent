import { JovoError } from '@jovotech/common';
export class CircularDependencyError extends JovoError {
    constructor(dependencyPath) {
        super({
            message: `Circular dependency detected: ${dependencyPath
                .map((x) => { var _a; return String((_a = x.name) !== null && _a !== void 0 ? _a : x); })
                .join(' -> ')}.`,
        });
        this.dependencyPath = dependencyPath;
    }
}
//# sourceMappingURL=CircularDependencyError.js.map