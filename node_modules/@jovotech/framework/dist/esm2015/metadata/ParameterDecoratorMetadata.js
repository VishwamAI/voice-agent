import { ClassDecoratorMetadata } from './ClassDecoratorMetadata';
export class ParameterDecoratorMetadata extends ClassDecoratorMetadata {
    // eslint-disable-next-line @typescript-eslint/ban-types
    constructor(target, index) {
        super(target);
        this.target = target;
        this.index = index;
    }
    hasSameTargetAs(otherMetadata) {
        return this.target === otherMetadata.target && this.index === otherMetadata.index;
    }
}
//# sourceMappingURL=ParameterDecoratorMetadata.js.map