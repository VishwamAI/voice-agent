var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { DEFAULT_INPUT_TYPE } from './index';
import { JovoInputBuilder } from './JovoInputBuilder';
var JovoRequest = /** @class */ (function () {
    function JovoRequest() {
    }
    JovoRequest.prototype.getInput = function () {
        return new JovoInputBuilder(this.getInputType() || DEFAULT_INPUT_TYPE)
            .set('intent', this.getIntent())
            .set('entities', this.getEntities())
            .set('text', this.getInputText())
            .set('audio', this.getInputAudio())
            .build();
    };
    JovoRequest.prototype.getSession = function () {
        var sessionId = this.getSessionId();
        var sessionData = this.getSessionData();
        var isNewSession = this.isNewSession();
        return !sessionId && !sessionData && typeof isNewSession === 'undefined'
            ? undefined
            : __assign(__assign({}, (isNewSession ? {} : sessionData || {})), { id: sessionId, isNew: isNewSession });
    };
    return JovoRequest;
}());
export { JovoRequest };
//# sourceMappingURL=JovoRequest.js.map