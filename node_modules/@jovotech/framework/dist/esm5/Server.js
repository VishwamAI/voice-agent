var Server = /** @class */ (function () {
    function Server() {
    }
    /**
     * Converts native header keys to lowercase
     *
     * Example:
     * headers = {
     *    Host: 'localhost:3000',
     *    Authorization: 'Bearer TOKEN',
     * }
     * Converts to:
     * headers = {
     *    host: 'localhost:3000',
     *    authorization: 'Bearer TOKEN',
     * }
     *
     */
    Server.prototype.getRequestHeaders = function () {
        var headers = this.getNativeRequestHeaders();
        return Object.keys(headers).reduce(function (destination, key) {
            destination[key.toLowerCase()] = headers[key];
            return destination;
        }, {});
    };
    return Server;
}());
export { Server };
//# sourceMappingURL=Server.js.map