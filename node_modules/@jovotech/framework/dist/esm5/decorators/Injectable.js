import { InjectableMetadata } from '../metadata/InjectableMetadata';
import { MetadataStorage } from '../metadata/MetadataStorage';
/**
 * Decorator to mark a class as injectable.
 * This allows the class to be automatically constructed by the dependency injection system.
 * @param options
 */
export function Injectable(options) {
    return function (target) {
        var metadata = new InjectableMetadata(target, options);
        var storage = MetadataStorage.getInstance();
        storage.addInjectableMetadata(metadata);
    };
}
//# sourceMappingURL=Injectable.js.map