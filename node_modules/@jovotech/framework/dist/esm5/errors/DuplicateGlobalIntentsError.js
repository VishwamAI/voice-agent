var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { JovoError } from '@jovotech/common';
export function buildDuplicateGlobalIntentsErrorMessage(entries, separator) {
    if (separator === void 0) { separator = '\n - '; }
    var duplicateEntriesText = entries
        .map(function (_a) {
        var intentName = _a[0], handlers = _a[1];
        var textBlocks = handlers.map(function (handler) { return handler.target.name + "." + handler.propertyKey.toString(); });
        var startText = textBlocks.slice(0, textBlocks.length - 1);
        var text = startText.join(separator) + " and " + textBlocks[textBlocks.length - 1];
        return intentName + " in " + text;
    })
        .join('; ');
    return "Duplicate global intent names detected:" + separator + duplicateEntriesText + ".";
}
// TODO improve to also display path of the components that cause the error
var DuplicateGlobalIntentsError = /** @class */ (function (_super) {
    __extends(DuplicateGlobalIntentsError, _super);
    function DuplicateGlobalIntentsError(entries) {
        return _super.call(this, {
            message: buildDuplicateGlobalIntentsErrorMessage(entries),
            hint: 'Check the intentMap as well.',
        }) || this;
    }
    return DuplicateGlobalIntentsError;
}(JovoError));
export { DuplicateGlobalIntentsError };
//# sourceMappingURL=DuplicateGlobalIntentsError.js.map