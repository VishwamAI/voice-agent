import { AudioUtilities } from './AudioUtilities';
var ParsedAudioInput = /** @class */ (function () {
    function ParsedAudioInput(samples, sampleRate) {
        this.samples = samples;
        this.sampleRate = sampleRate;
    }
    ParsedAudioInput.fromAudioInput = function (audio) {
        var samples = AudioUtilities.getSamplesFromBase64(audio.base64);
        return new ParsedAudioInput(samples, audio.sampleRate);
    };
    ParsedAudioInput.prototype.sampleDown = function (targetSampleRate) {
        this.samples = AudioUtilities.sampleDown(this.samples, this.sampleRate, targetSampleRate);
        this.sampleRate = targetSampleRate;
        return this;
    };
    ParsedAudioInput.prototype.toWav = function (targetSampleRate) {
        var samples = targetSampleRate
            ? AudioUtilities.sampleDown(this.samples, this.sampleRate, targetSampleRate)
            : this.samples;
        return AudioUtilities.encodeSamplesToWav(samples, targetSampleRate || this.sampleRate);
    };
    return ParsedAudioInput;
}());
export { ParsedAudioInput };
//# sourceMappingURL=ParsedAudioInput.js.map