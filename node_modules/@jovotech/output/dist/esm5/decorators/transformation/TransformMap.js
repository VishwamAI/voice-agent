import { plainToClass, Transform } from '../..';
export function TransformMap(typeFunction) {
    return Transform(function (_a) {
        var value = _a.value;
        var result = {};
        if (!value) {
            return result;
        }
        var entries = Object.entries(value);
        for (var i = 0, len = entries.length; i < len; i++) {
            var _b = entries[i], key = _b[0], obj = _b[1];
            result[key] = plainToClass(typeFunction(), obj);
        }
        return result;
    });
}
//# sourceMappingURL=TransformMap.js.map