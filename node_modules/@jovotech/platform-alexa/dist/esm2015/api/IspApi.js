import { JovoError } from '@jovotech/framework';
import { sendApiRequest } from './AlexaApi';
export async function getProductList(apiEndpoint, permissionToken, language, params) {
    const options = {
        endpoint: apiEndpoint,
        path: `/v1/users/~current/skills/~current/inSkillProducts`,
        permissionToken,
        headers: {
            'Accept-Language': language,
        },
        params,
    };
    try {
        const response = await sendApiRequest(options);
        return response.data;
    }
    catch (error) {
        if (error.isAxiosError) {
            const { message } = error.response.data;
            throw new JovoError({ message });
        }
        throw new JovoError({ message: error.message });
    }
}
//# sourceMappingURL=IspApi.js.map