import { Configurable, MarketplacePlugin, SupportedLanguages } from '@jovotech/cli-core';
/**
 * Downloads and extracts a template.
 * @param projectPath - Path to download and extract the template to.
 */
export declare function downloadTemplate(projectPath: string, language: SupportedLanguages): Promise<void>;
export declare function runNpmInstall(projectPath: string): Promise<void>;
/**
 * Inserts a substring into a provided string at an index.
 * @param substr - Substring to be inserted.
 * @param str - String to insert the substring into.
 * @param index - Position of where to insert the substring.
 */
export declare function insert(substr: string, str: string, index: number): string;
/**
 * Gets plugins from Jovo Marketplace.
 */
export declare function fetchMarketPlace(): MarketplacePlugin[];
export declare function loadPlugin(projectPath: string, pkg: string, module: string): Configurable;
export declare function getFormattedPluginInitConfig(plugin: Configurable): Promise<string>;
