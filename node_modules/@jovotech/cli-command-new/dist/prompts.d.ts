import { MarketplacePlugin, Preset, ProjectProperties, prompt } from '@jovotech/cli-core';
import { NewContext } from './commands/new';
export declare function promptPreset(presets: Preset[]): Promise<{
    selectedPreset: string;
}>;
export declare function promptProjectProperties(args: NewContext['args'], flags: NewContext['flags']): Promise<ProjectProperties>;
export declare function promptSavePreset(): Promise<{
    savePreset: boolean;
}>;
export declare function promptPresetName(): Promise<{
    presetName: string;
}>;
export declare function promptServer(servers: prompt.Choice[]): Promise<{
    server: MarketplacePlugin | undefined;
}>;
export declare function promptPlugins(message: string, plugins: prompt.Choice[]): Promise<{
    plugins: MarketplacePlugin[];
}>;
