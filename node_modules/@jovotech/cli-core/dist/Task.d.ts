export declare type TaskFunction = () => string[] | string | void | Promise<string[] | string | void>;
export interface TaskConfig {
    enabled: boolean;
    indentation: number;
}
export declare class Task {
    private readonly title;
    private readonly action;
    private readonly config;
    private spinner?;
    constructor(title: string, action?: Task[] | TaskFunction, config?: Partial<TaskConfig>);
    getDefaultConfig(): TaskConfig;
    add(...actions: Task[]): void;
    indent(indentation: number): void;
    enable(): void;
    disable(): void;
    run(): Promise<void>;
}
