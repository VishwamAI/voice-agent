/**
 * Prompt if existing model files should be overwritten.
 */
export declare function promptOverwriteReverseBuild(): Promise<{
    overwrite: string;
}>;
/**
 * Prompt for overwrite.
 * @param message - Message to display on prompt.
 */
export declare function promptOverwrite(message: string): Promise<{
    overwrite: string;
}>;
export declare function promptSupportedLocales(locale: string, platform: string, supportedLocales: string[]): Promise<{
    locales: string[];
}>;
